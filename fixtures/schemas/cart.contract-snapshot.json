{
  "_comment": "Captured contract snapshot of GET /carts/:id response shape. The schema/contract test asserts future live responses still conform to this exact key-set (no silently added/removed fields), in addition to full Ajv schema + type validation.",
  "topLevelKeys": ["id", "userId", "date", "products", "__v"],
  "productItemKeys": ["productId", "quantity"],
  "types": {
    "id": "integer",
    "userId": "integer",
    "date": "string",
    "products": "array",
    "__v": "integer"
  }
}
